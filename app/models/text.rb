class Text < ApplicationRecord
    # validates :title_fr, presence: true
    # validates :author, presence: true
    # validates :content_fr, presence: true

    # scope :content_fr, -> { where(state: "approved") }
end
